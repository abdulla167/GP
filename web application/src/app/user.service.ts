import {User} from './models/user.model';

export class UserService{
  registerUser(user : User){
    // send user to be saved in the database
  }

  authenicateUser(user :User){
    // send user to back-end to athenicate and get his data
  }
}
